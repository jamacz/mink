= std.op

; Skips the first item on the stack
& $ a ?= & a ! : a ?

; Pops the top item off the stack
\ $ ?= \ :

; Adds top two items of the stack
+ $ ?= + ! :

; Clones the top of the stack
* $ ?= &(??) *_
*_ $ = &(&! !) *_ :

; Subtracts top of the stack from item second on stack
- $ ?= &(:?) - :

; Swaps the top two items on the stack
/ $ ?= & &? /_
/_ $ = & &! /_: